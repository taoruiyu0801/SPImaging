# SPImaging 命令行功能清单 V1

## 1. 使用前准备

在 PowerShell 中进入项目并激活环境：

```powershell
cd <SPImaging项目目录>
conda activate spimaging
```

查看某个命令的全部参数：

```powershell
spad-generate --help
```

## 2. 命令速查

| 命令 | 功能 |
| --- | --- |
| `spad-generate` | 从 NYUv2 或 Middlebury RGB-D 数据生成 SPAD `.npz` 样例 |
| `spad-verify` | 检查单个样例，显示 RGB、深度、count map 和时间直方图 |
| `spad-browse` | 使用 OpenCV 交互浏览多个 SPAD 样例 |
| `spad-train` | 训练 Simple3D、PRSNet、PENonLocal 或 STIN 监督模型 |
| `spad-train-selfsup` | 训练自监督 SPISR 模型 |
| `spad-predict` | 使用 checkpoint 对一个样例进行深度预测 |
| `spad-evaluate` | 批量评估一个或多个 checkpoint |
| `spad-demo` | 无界面串行完成样例检查、最小训练、预测和评估 |
| `spad-browse-4modes` | `spad-browse` 的兼容别名，功能相同 |

### 统一参数与路径规则

- 参数名统一使用下划线风格；推荐使用 `--sample_file`、`--sample_name`、`--output_dir`、`--output_npz` 和 `--output_fig`。
- `--sample`、`--save_fig`、`--save_dir` 作为旧名称暂时兼容。
- 输入路径必须存在，不会自动创建；输出目录或输出文件父目录只在输入校验通过后自动创建。
- 已有输出默认不覆盖；确认替换时使用 `--overwrite`。错误输入以状态码 2 退出且不显示 Python traceback。
- 完整默认值、合法范围和路径行为见 `record_of_SPI/Day_13-14/参数说明表.xlsx`；同目录另提供 Markdown/CSV 版本。

## 3. `spad-generate`：生成数据

输入 RGB-D 数据，输出 `sample_*.npz` 和 `index.csv`。

```powershell
spad-generate `
    --dataset_mode middlebury `
    --middlebury_root middlebury/raw `
    --surface_model neighborhood_mix `
    --output_dir outputs/generated_data `
    --limit 2
```

常用测量模型：

```text
single
neighborhood_mix
translucent_layer
volume_scattering
```

## 4. `spad-verify`：检查单个样例

读取一个 `.npz`，显示并保存 RGB、深度、光子 count map 和时间直方图。

```powershell
spad-verify `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --index 0 `
    --output_fig outputs/verify_sample_00000.png
```

## 5. `spad-browse`：交互浏览数据

打开 OpenCV 窗口，逐个浏览数据集中的样例。

```powershell
spad-browse `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --browse_mode auto
```

常用按键：

| 按键 | 功能 |
| --- | --- |
| `A` / `D` | 上一个 / 下一个样例 |
| `R` | 随机样例 |
| `G` | 保存当前画布 |
| `I` | 打印样例信息 |
| `Q` / `Esc` | 退出 |

## 6. `spad-train`：监督训练

使用 `.npz` 数据训练 SPAD 深度重建模型，并保存 `best.pt` 和 `last.pt`。

```powershell
spad-train `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --output_dir outputs/train_simple3d `
    --epochs 1 `
    --batch_size 1 `
    --model simple3d `
    --base_channels 2 `
    --temporal_downsample 64 `
    --num_workers 0
```

支持的模型：

```text
simple3d
prsnet
penonlocal
stin
```

## 7. `spad-train-selfsup`：自监督训练

使用 photon cube 训练自监督 SPISR，不需要高分辨率标签。

```powershell
spad-train-selfsup `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --output_dir outputs/train_spisr `
    --epochs 1 `
    --batch_size 1 `
    --max_samples 2 `
    --base_channels 4 `
    --num_blocks 1 `
    --temporal_downsample 64 `
    --spatial_downsample 2
```

## 8. `spad-predict`：单样本预测

加载训练得到的 checkpoint，对一个 SPAD 样例预测深度。

```powershell
spad-predict `
    --checkpoint outputs/train_simple3d/best.pt `
    --sample_file example_data/nyuv2_raw_single_random_snr/sample_00000.npz `
    --output_npz outputs/prediction/sample_00000.npz `
    --output_fig outputs/prediction/sample_00000.png
```

## 9. `spad-evaluate`：批量评估

在整个数据集上评估 checkpoint，输出 MAE、RMSE、AbsRel 和对比图。

```powershell
spad-evaluate `
    --checkpoint outputs/train_simple3d/best.pt `
    --label simple3d `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --output_dir outputs/evaluate_simple3d
```

评估目录中会生成：

```text
metrics_per_sample.csv
metrics_summary.json
comparison.png
```

## 10. `spad-demo`：一键稳定演示

使用仓库自带样例，以轻量配置依次完成“检查样例—最小训练—预测—评估”：

```powershell
spad-demo
```

也可以指定数据集和输出目录：

```powershell
spad-demo `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --output_dir outputs/demo
```

默认输出到 `outputs/demo`，包括检查图、训练 checkpoint、预测结果、评估指标、`demo.log` 和 `demo_summary.json`。全流程使用无界面绘图后端，不会打开窗口；已有非空输出目录默认拒绝覆盖，需要重跑时使用 `--overwrite`。

如果只演示预训练模型的预测和评估，可使用仓库内置的小型数据与 Day 19 checkpoint：

```powershell
conda run -n spimaging spad-predict `
    --checkpoint demo_checkpoint/simple3d_demo_best.pt `
    --sample_file example_data/nyuv2_raw_single_random_snr/sample_00000.npz `
    --output_npz outputs/day19_quick_demo/prediction.npz `
    --output_fig outputs/day19_quick_demo/comparison.png

conda run -n spimaging spad-evaluate `
    --checkpoint demo_checkpoint/simple3d_demo_best.pt `
    --label day19-simple3d `
    --dataset_dir example_data/nyuv2_raw_single_random_snr `
    --output_dir outputs/day19_quick_demo/evaluate
```

命令入口位于 Conda `spimaging` 环境；未激活该环境时使用上述 `conda run -n spimaging` 形式。该演示数据和模型仅用于快速验证，不代表正式训练或独立测试集精度。

## 11. 推荐使用顺序

```text
快速演示：spad-demo

手动流程：spad-generate
          → spad-verify / spad-browse
          → spad-train 或 spad-train-selfsup
          → spad-predict
          → spad-evaluate
```
